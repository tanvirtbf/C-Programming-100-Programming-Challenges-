#include <stdio.h>
// Create a program to print the Fibonacci series up to a certain number

int main(){
    int num;
    printf("Enter a Number : ");
    scanf("%d",&num);
    printf("%d",num);
    if(num>0){
        printf("%d",1);
    }
    return 0;
}
