#include <stdio.h>
// Create a program to print the Fibonacci series up to a certain number

int main(){
    int x, result=0;
    printf("Enter a Number : ");
    scanf("%d",&x);
    while(result<x){

    }
    return 0;
}
